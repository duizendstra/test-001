# Git repository files
.git/
.gitignore

# Docker files (not needed inside the image context)
Dockerfile
.dockerignore

# Go build artifacts (the Dockerfile builds its own)
bin/
*.test
*.prof
coverage.out

# IDE / Editor / OS specific files
.vscode/
.idea/
*.iml
.DS_Store
Thumbs.db

# Environment / Secrets files (VERY IMPORTANT)
.env*

# Task runner / AI context files
ai_context.txt

# Nix / Direnv environment artifacts (if used locally)
.direnv/
result*

# Local log files
*.log
crash.*.log

gcp-sa-key.json
notes.txt
